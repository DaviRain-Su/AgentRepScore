## Codebase Patterns
- **Script entry guard**: All new CLI scripts in `scripts/` should use `isMainModule()` (same pattern as `src/cli.ts` and `src/server.ts`) to prevent accidental execution during test imports.
- **Keeper submission utilities**: Use `src/skill/keeper-utils.ts` for all on-chain `submit*` calls. It provides exponential-backoff retry (`submitWithRetry`), evidence-hash deduplication (`isAlreadySubmitted`), and local state persistence (`loadKeeperState` / `saveKeeperState`).
- **Keeper state file**: The local deduplication state file path defaults to `.keeper-state.json` and can be overridden with the `KEEPER_STATE_PATH` env var.
- **Testnet RPC sensitivity**: `test/hardhat/integration.test.ts` runs against live X Layer testnet and will timeout/hang if the RPC is slow or unreachable. This can block `pnpm test:all` even when local/unit changes are correct.
- **Pool address configuration**: Uniswap V3 pool addresses are not hardcoded; they must be supplied via `UNISWAP_POOLS` env var as comma-separated addresses.

# Ralph Progress Log
Started: 2026年 4月11日 星期六 23时28分07秒 CST
---

## 2026-04-12 01:45 CST - US-004
Tool: pi
- Migrated contract governance to a Safe-compatible multisig by updating deployment scripts and adding Foundry tests.
- Created `contracts/mocks/MockMultisig.sol`, a lightweight 2-of-3 multisig mock that approves and executes arbitrary calls. It emits `Approved`/`Executed` events and reverts on insufficient approvals or failed executions.
- Added `test/foundry/MultisigGovernance.t.sol` with 6 tests demonstrating that `scheduleRegisterModule`, `executeRegisterModule`, `scheduleUpdateWeight`, `executeUpdateWeight`, `initiateGovernanceTransfer`, `pause`, and `setEvaluator` all work when `governance` is the mock multisig address.
- Updated `scripts/deploy-mainnet.ts` and `scripts/redeploy-validator-testnet.ts` to read `GOVERNANCE_SAFE` from `.env` and pass it as the initial `governance_` constructor argument (falling back to `deployer` when unset).
- Updated `.env.example` with a `GOVERNANCE_SAFE=` placeholder.
- Updated root `AGENTS.md` with governance-multisig patterns.
- All quality checks pass: `forge test` (83 tests), `pnpm tsc --noEmit`, `npx vitest run test/skill/` (55 tests).
- Files changed: `contracts/mocks/MockMultisig.sol`, `test/foundry/MultisigGovernance.t.sol`, `scripts/deploy-mainnet.ts`, `scripts/redeploy-validator-testnet.ts`, `.env.example`, `AGENTS.md`, `prd.json`
- **Learnings for future iterations:**
  - `AgentRepValidator` does not need code changes to support a Gnosis Safe as governance; any address with `onlyGovernance` checks is automatically compatible. The migration is purely a deployment-level change.
  - For Foundry tests, a simple `_multisigExecute(target, data)` helper that calls `approve` twice and then `execute` on `MockMultisig` keeps multisig test code concise and readable.
  - Deployment scripts currently use `ethers` v6 (`JsonRpcProvider`, `ContractFactory`). When adding new constructor args or env vars, remember to update both `deploy-mainnet.ts` and `redeploy-validator-testnet.ts` to keep deployment paths consistent.
---

## 2026-04-11 23:55 CST - US-001
Tool: pi
- Implemented `scripts/indexer-uniswap.ts` that listens to Uniswap V3 `Swap` events from pools configured via `UNISWAP_POOLS` env var on X Layer testnet.
- The indexer aggregates `swapCount`, `volumeUSD`, `netPnL`, `avgSlippageBps`, and generates a `SwapSummary` compatible with `UniswapScoreModule.submitSwapSummary`.
- Added `test/skill/indexer-uniswap.test.ts` with unit tests for `parsePools`, `buildSwapSummary`, and slippage calculations using mock swap events.
- Updated `.env.example` to include `UNISWAP_POOLS` placeholder.
- Applied `isMainModule()` guard to `scripts/indexer-uniswap.ts` to prevent process.exit on import during tests.
- Files changed: `scripts/indexer-uniswap.ts`, `test/skill/indexer-uniswap.test.ts`, `.env.example`, `prd.json`
- **Learnings for future iterations:**
  - `pnpm test:all` currently hangs on `test/hardhat/integration.test.ts` when the X Layer testnet RPC is unresponsive. Run `npx vitest run test/skill/` and `forge test` separately to verify local changes if the testnet RPC is down.
  - When creating new runnable scripts in `scripts/`, always wrap the `main()` call with `isMainModule()` so vitest can import the module without side-effects.
  - Uniswap V3 Swap events contain `sqrtPriceX96` which can be converted to an implied price ratio using `(sqrtPriceX96^2) / 2^192` — useful for slippage approximations in the indexer.
---

## 2026-04-12 01:20 CST - US-002
Tool: pi
- Implemented `scripts/keeper-daemon.ts`, a `setInterval`-based scheduled daemon that auto-runs keeper submissions every 5 minutes (configurable via `DAEMON_INTERVAL_MS`).
- Created `scripts/keeper-aave.ts` to fill the missing wallet-meta keeper gap; it submits `liquidationCount` and `suppliedAssetCount` to `AaveScoreModule`.
- The daemon discovers which keepers to run based on `.env` configuration:
  - Swap: `UNISWAP_MODULE` + `UNISWAP_POOLS` → `indexer-uniswap.ts`
  - Activity: `BASE_MODULE` + OKX credentials → `keeper-oklink.ts`, otherwise `keeper-rpc.ts`
  - Wallet Meta: `AAVE_MODULE` → `keeper-aave.ts`
- Added `test/skill/keeper-daemon.test.ts` covering `parseWallets` and `buildKeeperCommands` logic.
- Updated `.env.example` with `WALLETS` and `DAEMON_INTERVAL_MS` placeholders.
- Added README section under Keeper scripts explaining how to start/stop the daemon.
- Added `isMainModule()` guard pattern note to root `AGENTS.md`.
- Files changed: `scripts/keeper-daemon.ts`, `scripts/keeper-aave.ts`, `test/skill/keeper-daemon.test.ts`, `.env.example`, `README.md`, `AGENTS.md`, `prd.json`
- **Learnings for future iterations:**
  - When building a daemon that wraps existing scripts, spawning `npx tsx scripts/...` with `stdio: "inherit"` is the lowest-friction approach because it reuses the existing keeper CLIs without refactoring their `process.exit` flows.
  - `buildKeeperCommands` accepts an optional `env` parameter so tests can inject configuration without mutating global `process.env`.
  - There was no existing Aave wallet-meta keeper; when the acceptance criteria reference "existing keeper logic" that doesn't yet exist, create the missing keeper script as part of the story.
---

## 2026-04-12 01:40 CST - US-003
Tool: pi
- Created `src/skill/keeper-utils.ts` providing reusable `submitWithRetry` (exponential backoff, up to 3 retries), `isAlreadySubmitted` / `recordSubmission` for evidence-hash deduplication, and `loadKeeperState` / `saveKeeperState` for local JSON state persistence.
- Refactored all four keeper scripts to use the shared utilities:
  - `scripts/indexer-uniswap.ts` — checks deduplication before `submitSwapSummary` and records `receipt.blockNumber` on success
  - `scripts/keeper-oklink.ts` — same pattern for `submitActivitySummary`; also added missing `isMainModule()` guard
  - `scripts/keeper-rpc.ts` — same pattern for `submitActivitySummary`; also added missing `isMainModule()` guard
  - `scripts/keeper-aave.ts` — computes a local `evidenceHash` from inputs (since `submitWalletMeta` has no on-chain evidence field), then deduplicates and records state
- Added `KEEPER_STATE_PATH` to `.env.example` (defaults to `.keeper-state.json`).
- Added `test/skill/keeper-utils.test.ts` with 12 tests covering state load/save, deduplication, hash cap (100 entries), retry success/failure, and sleep.
- All quality checks pass: `pnpm tsc --noEmit`, `npx vitest run` (58 tests), `forge test` (77 tests).
- Files changed: `src/skill/keeper-utils.ts`, `scripts/indexer-uniswap.ts`, `scripts/keeper-oklink.ts`, `scripts/keeper-rpc.ts`, `scripts/keeper-aave.ts`, `test/skill/keeper-utils.test.ts`, `.env.example`, `AGENTS.md`, `prd.json`
- **Learnings for future iterations:**
  - `keeper-oklink.ts` and `keeper-rpc.ts` were missing `isMainModule()` guards; any runnable script in `scripts/` must have it so vitest can import the module safely.
  - For keeper deduplication, the `evidenceHash` already present in summary structs is the perfect unique key. When a module lacks an evidence field (e.g., `AaveScoreModule.submitWalletMeta`), compute a local keccak256 hash from the normalized arguments.
  - Centralizing retry + deduplication in `src/skill/keeper-utils.ts` keeps individual keeper scripts focused on data gathering while ensuring consistent resilience across all submission paths.
---
